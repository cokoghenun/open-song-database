/**  @jsx jsx  */
import { jsx } from '@emotion/core';
import Page from '../../components/Page';

const Parameters = ({ match }) => {
  return (
    <div>
      <Page title='parameters' content='Parameters' />
    </div>
  );
};
export default Parameters;
